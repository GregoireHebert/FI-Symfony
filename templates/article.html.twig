<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <title>{% block title %}Un blog magnifique{% endblock %}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    </head>
        {{ include('header.html.twig') }}
    <body style="width: 100%;position: absolute;top: 50px;margin-bottom: 10px;">
        {% block body %}
            <div style="text-align: center;margin-top: 50px;">
                <h1>{{ article.title }}</h1>
                <h3>{{ article.subtitle }}</h3>
                <p>Publié le : {{ article.createdAt | date("d/m/Y") }}</p>
                <div style="display: flex; align-items: center; justify-content: center;">
                    {% for tag in article.tags %}
                        <p style="margin-right: 10px;">{{ tag.name }}</p>
                    {% endfor %}
                </div>
                <div>{{ article.corpus }}</div>
            </div>
            <div style="margin-top: 50px; text-align: center;">
                <a href="{{ path('home') }}">Retour</a>
            </div>
            <div style="margin-top: 20px; display: flex; align-items: center; justify-content: center;">
                {% if article.id > 1%}<a style="margin-right: 10px;" href="{{ path('getArticle', {'id': article.id-1}) }}">Article Précédent</a>{% endif %}
                {% if article.id < size%}<a href="{{ path('getArticle', {'id': article.id+1}) }}">Article Suivant</a>{% endif %}
            </div>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
        {{ include('footer.html.twig') }}
</html>
